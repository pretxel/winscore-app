## 1. Remove quiz link from site navigation

- [x] 1.1 Remove `{ href: lp("/quiz"), label: t("quiz") }` from the `links` array in `components/site-nav.tsx`
- [x] 1.2 Run `tsc --noEmit` and verify no type errors
- [x] 1.3 Run `biome check` and verify no lint errors
- [x] 1.4 Run `vitest run` and verify tests pass
